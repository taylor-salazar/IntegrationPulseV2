{
	"name": "integration-pulse",
	"version": "0.1.0",
	"private": true,
	"description": "Integration Pulse — SAPUI5 operations console for deployed SAP Integration Suite integrations",
	"scripts": {
		"start": "node scripts/prepare-development.cjs --local && ui5 serve --config .ui5/ui5.development.yaml --open \"index.html?mock=true\"",
		"start:mock": "npm start",
		"start:live": "npm run start:destination",
		"start:destination": "node scripts/prepare-development.cjs && ui5 serve --config .ui5/ui5.development.yaml --open index.html",
		"start:proxy": "node scripts/prepare-development.cjs --local && ui5 serve --config .ui5/ui5.development.yaml --open \"index.html?mock=false&api=proxy\"",
		"serve": "ui5 serve --port 8080",
		"build": "ui5 build --clean-dest",
		"test": "npm run test:frontend && npm run test:backend",
		"test:frontend": "node --test tests/js/*.test.cjs",
		"test:backend": "python -m unittest discover -s tests/backend -p test_*.py -v"
	},
	"devDependencies": {
		"@sap/ux-ui5-tooling": "^1.20.3",
		"@ui5/cli": "^3.11.0",
		"@xmldom/xmldom": "0.8.10"
	}
}
